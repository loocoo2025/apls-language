# 既有软件项目规范化迁移指南
## 将正在开发中的项目迁移到规范化 AI 软件工程体系

> 用途：已有代码、文档、测试或现场版本，但目录与工程流程不规范的项目。
>
> 目标：**不推倒重来，在尽量不影响现有功能、构建、部署、接口、数据和交付的前提下，逐步迁移到“迭代式 V 模型 + 需求追溯 + ADR + AI 独立评审 + 自动化验证 + 多对话协作”体系。**

---

# 0. 迁移硬规则

1. 第一阶段只读盘点，禁止立即大规模改代码或移动目录。
2. 不因为目录乱就重写能工作的代码。
3. 不为“整齐”破坏构建、相对路径、资源路径、部署脚本或兼容性。
4. 无法确认用途的文件不得删除，统一标记 `UNKNOWN`。
5. 现有代码行为不能自动认定为正式需求。
6. 旧文档也不能自动认定为当前有效基线，必须判断是否过期。
7. 迁移前必须建立 Git 可回退点；没有 Git 时先完整备份。
8. 必须区分 `As-Is`（现在真实状态）与 `To-Be`（目标状态）。
9. 先建立逻辑规范，再考虑物理目录整理。
10. 工程规范化、技术栈升级、大型重构必须分开立项。
11. 每批移动/重构后必须重新 Build + Test + 必要运行验证。
12. 不确定事项必须标记 `UNKNOWN / INFERRED / OPEN`，禁止 AI 编造。
13. 从一个明确的基线 Commit 起，所有新开发必须按新规范执行。
14. 旧债允许逐步偿还，但禁止继续制造同类新债。
15. 迁移目标是“以后可控”，不是“一次性完美”。

# 1. 迁移前安全点

如果使用 Git：

```text
确认工作区
→ 提交应保留修改
→ 记录当前 Commit
→ 建立迁移前 Tag
→ 建立 migration/engineering-standardization 分支
```

推荐 Tag：`pre-standardization-YYYYMMDD`

记录：当前版本、Commit、分支、构建结果、运行结果、测试结果、部署版本、已知 Bug、未提交修改。

# 2. 第一轮只做项目盘点

AI 第一次进入旧项目，只允许阅读、搜索、分类、记录和形成计划。

必须先回答：
- 项目做什么；
- 怎么构建；
- 怎么运行；
- 怎么部署；
- 主入口在哪里；
- 模块有哪些；
- 文档有哪些；
- 测试有哪些；
- CI/静态分析/Sanitizer 有没有；
- 配置、数据库、脚本、资源在哪里；
- 哪些是第三方/自动生成；
- 哪些用途未知；
- 哪些内容移动后最危险。

必须建立：

```text
00_project/migration/
├── LEGACY_INVENTORY.md
├── LEGACY_STRUCTURE_MAP.md
├── UNKNOWN_ITEMS.md
├── MIGRATION_PLAN.md
├── MIGRATION_LOG.md
└── MIGRATION_RISKS.md
```

# 3. 旧文件机械分类

每项只能先归入下列之一：

```text
A CURRENT_VALID        当前有效
B VALID_WRONG_PLACE    有效但位置不规范
C HISTORY_REFERENCE    历史参考
D DEPRECATED_KEEP      已废弃但需保留记录
E GENERATED            自动生成
F THIRD_PARTY          第三方
G UNKNOWN              用途未知
H DELETE_CANDIDATE     可删除候选，尚未批准
```

`G/H` 不得由 AI 擅自删除。

# 4. 先做逻辑映射，不急着移动

`LEGACY_STRUCTURE_MAP.md` 至少包含：

| 旧位置 | 内容 | 当前状态 | 新体系目标位置 | 当前处理 |
|---|---|---|---|---|
| doc/spec.md | 系统需求 | 部分有效 | 02_system_requirements/ | 提炼，原件先保留 |
| src/net/ | 网络模块 | 有效 | 07_src/modules/network/ | 暂不移动 |

**建立映射不等于立即物理移动。**

# 5. 默认迁移阶段

```text
M0 安全快照
M1 旧项目盘点
M2 加入工程规则和 AI 上下文控制文件
M3 重建当前需求基线
M4 重建 As-Is 架构
M5 建立 ADR 和需求追溯
M6 建立测试与质量基线
M7 设置新流程基线 Commit
M8 小批量物理目录整理
M9 长期技术债治理
```

禁止从 M8 开始。

# 6. 重建需求基线

旧项目可能出现“代码、文档、测试、现场认知四套说法”。每条需求必须标状态：

- `CONFIRMED`：项目负责人已确认；
- `INFERRED`：从旧系统推断，尚未确认；
- `OPEN`：存在冲突，需要质询。

AI 不得把 `INFERRED` 自动升级为 `CONFIRMED`。

# 7. 先重建 As-Is 架构

先写真实存在的模块、线程、进程、数据流、网络流、生命周期、配置、部署和依赖。

推荐：

```text
03_architecture/
├── as_is_architecture.md
├── target_architecture.md
└── architecture_decisions/
```

只有确认要改时才设计 `To-Be`，并通过 ADR 记录原因。

# 8. 历史 ADR 禁止编故事

能确认原因的正常补 ADR；无法确认的写：

```text
状态：Accepted Legacy
历史理由：Partially Unknown
当前事实：...
已知约束：...
重新评估条件：...
```

# 9. 建立追溯，允许阶段性缺口

```text
需求 → 架构/设计 → 代码模块 → 测试
```

允许：`MAPPED / PARTIAL / MISSING / UNKNOWN`。禁止为了表格好看编造映射。

# 10. 建立测试基线

优先补测试区域：

```text
失败代价高 + 历史 Bug 多 + 高频修改 + 边界复杂
```

不要求迁移当天覆盖所有历史代码。原则：**从新基线开始，新功能必须有测试；历史高风险模块逐步补。**

# 11. 设置新工程基线

记录：

`STANDARDIZATION_BASELINE_COMMIT = <commit>`

从此以后：新需求编号、新 ADR、新验收标准、新 Bug 问题单、Bug 回归测试、新接口文档、新重大变更影响分析、多对话角色分工全部强制执行。

# 12. 物理目录整理最后做

移动前检查：CMake/Make/qmake/Meson/Bazel、import、Qt resource、相对路径、配置、安装脚本、CI、Docker、systemd、测试脚本、打包脚本、外部依赖路径。

每批只移动一个逻辑模块：

```text
移动 → 修构建 → Build → Test → 运行验证 → Commit
```

如果移动收益低、风险高，可以永久保留旧物理位置，只做逻辑规范化。

# 13. 迁移期任务必须标类型

- `[PRODUCT]` 产品开发
- `[STANDARDIZATION]` 工程规范化

禁止把大型重构偷偷夹在“整理目录”任务中。

# 14. 迁移期间禁止顺手升级所有技术栈

Qt/C++/Ubuntu/数据库/网络库/构建系统升级必须单独立项。工程规范化不是技术升级许可证。

# 15. 发现严重问题时升级处理

发现数据丢失、安全漏洞、严重并发风险、大规模泄漏、协议根本错误、架构无法满足已确认需求时：

```text
建立 BUG/CR/风险记录
→ 收集证据
→ C02 设计处理
→ C04 独立评审
→ C05 验证
→ 决定整改计划
```

# 16. 旧项目迁移也使用 7 个对话

```text
C00 总控/迁移计划
C01 重建需求基线
C02 As-Is/To-Be 架构与设计
C03 必要代码与目录调整
C04 独立迁移/设计评审
C05 测试、CI、验证基线
C06 历史 Bug、现场问题、变更闭环
```

# 17. 基本完成标准

- [ ] 工程规则存在
- [ ] CURRENT_STATE 能让新 AI 快速理解项目
- [ ] 当前需求基线存在
- [ ] 当前 As-Is 架构存在
- [ ] 关键 ADR 已记录
- [ ] 关键需求有基本追溯
- [ ] 测试能力和缺口已知
- [ ] 新开发已切换新规范
- [ ] Bug/变更有闭环
- [ ] 多对话/HANDOFF 已启用
- [ ] 构建、运行、部署未被迁移破坏
- [ ] 历史未完成治理项已记录

# 18. 直接给 AI 的迁移启动指令

```text
现在这个项目已经在开发中，但目录、文档、需求、设计、测试和工程流程不完全符合我们的标准。

你的任务不是推倒重写，而是在保证现有功能、构建、部署、接口、数据和正在进行的开发尽量不受影响的前提下，把项目逐步迁移到规范化软件工程体系。

先完整阅读 `AI_START_HERE.md`，严格遵循它维护的权威启动顺序，再阅读 `AI_LEGACY_PROJECT_STANDARDIZATION_GUIDE.md`。本提示词不维护另一份竞争性的启动顺序。

第一阶段只允许只读盘点，禁止大规模修改代码、移动目录、架构重写或技术栈升级。

先提交：
A. 技术栈、构建、运行、部署、Git 状态
B. 当前目录与主要文件分类
C. 现有需求/设计/代码/测试/CI/部署/发布资料
D. 明显工程问题
E. UNKNOWN 文件或目录
F. 迁移最主要风险
G. 分阶段迁移计划
H. 需要 Expert Escalation 的 P0/P1 问题
I. Expert 无法在 Current Truth 与现有授权内解决、必须由我确认的事项

所有不确定事项标记 UNKNOWN / INFERRED / OPEN。
P0/P1 不自动等同于人工阻塞；先按工程总则交给 Expert，只有需要改变 Current Truth、重大 Gate 或风险接受时才请求项目负责人。
完成以上分析前，不得执行大规模变更。
```
